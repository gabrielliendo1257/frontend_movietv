export class Media {
    constructor(title: string, public description: string, public uriImage: string, public videoUrl: string) {
    }
}

export class FileUpload {
    constructor(filename: string) {
    }
}
